{
  "buildCommand": "npm run build:client",
  "installCommand": "pnpm install --frozen-lockfile",
  "public": "dist/spa",
  "rewrites": [
    {
      "source": "/((?!api).*)",
      "destination": "/index.html"
    }
  ]
}
